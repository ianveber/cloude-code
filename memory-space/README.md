# Memory Space

Last updated: 2026-09-17

Local Obsidian-style viewer for `_claude-memory/`. Notes are colored by who wrote them:

- **Claude** — orange
- **ChatGPT** — blue
- **Cursor** — purple

```bash
./scripts/memory-space.sh
```

Then open `http://127.0.0.1:8765/memory-space/`. Rebuild the index after memory edits with `python3 scripts/build-memory-space.py`.

Private. Do not deploy this folder publicly — it contains the same notes as the private vault.
